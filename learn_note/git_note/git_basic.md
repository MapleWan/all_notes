本机配置
```shell
git config --global user.email “1041709112@qq.com”
git config --global user.name “MapleWan”
```

进入本地文件夹
```shell
git init
git add .   # 这里的add .  指的是添加所有的文件  可以在这里指定文件
git commit -m "提交信息"
git remote add origin https://github.com/MapleWan/all_notes.git
git push origin master
```